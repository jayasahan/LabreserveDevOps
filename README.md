# LabReserve

LabReserve is a simple MERN web application for laboratory equipment requests and management.

This repository is initialized with:

- `client/` for the React and Vite frontend
- `server/` for the Node.js and Express backend

Implementation will stay aligned with `AGENTS.md`.
